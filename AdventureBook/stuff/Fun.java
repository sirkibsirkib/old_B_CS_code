package stuff;

public class Fun {
	public static double rrg(double max){//random real generate
		return Math.random()*max;
	}

	public static int rng(int range) {
		return (int)(Math.random()*range);
	}
}
